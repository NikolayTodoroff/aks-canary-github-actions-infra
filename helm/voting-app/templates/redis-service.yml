{{- if eq .Values.track "stable" }}
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ .Values.namespace | default "default" }}
spec:
  selector:
    app: {{ .Values.redis.app | default "redis" }}
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
{{- end }}